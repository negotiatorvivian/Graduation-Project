#!/usr/bin/env python
# -*- coding:utf-8 -*-

import os
import sys
import socket

# reload(sys)
# sys.setdefaultencoding('utf-8')


base_dir = os.path.dirname(sys.path[0])
sys.path.insert(1, base_dir)

# fname = '%s/conf/log_conf.ini' % (base_dir)
# defaults = {'logs_path': '%s/logs' % base_dir}
# logging.config.fileConfig(fname = fname, defaults = defaults)
